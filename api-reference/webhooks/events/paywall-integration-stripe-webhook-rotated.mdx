---
title: "Stripe webhook rotated"
description: "Triggered when a new Stripe webhook endpoint is created for a paywall.  Keeps billing operators in sync when Stripe webhook endpoints are recreated or rotated.  **Payload fields**: `paywallId`, `ownerUserId`, `webhookId`, `url`, `secretSuffix`."
openapi: "Webhook paywall.integration.stripe.webhook.rotated"
---
